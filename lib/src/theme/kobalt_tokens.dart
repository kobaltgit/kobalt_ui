import 'package:flutter/material.dart';

class KobaltTokens {
  // Базовые фоны и поверхности (Dark Mode)
  static const Color background = Color(0xFF060911);
  static const Color surface = Color(0xFF0D1322);
  static const Color surfaceHover = Color(0xFF151E34);
  static const Color surfaceBorder = Color(0x1FFFFFFF);
  static const Color surfaceBorderSubtle = Color(0x0FFFFFFF);

  // Текстовые цвета
  static const Color textPrimary = Color(0xFFF8FAFC);
  static const Color textSecondary = Color(0xFF94A3B8);
  static const Color textMuted = Color(0xFF64748B);

  // Шрифты
  static const String fontFamily = 'Segoe UI';

  // Размеры
  static const double maxWidth = 1200.0;
  static const double navBarHeight = 68.0;
}
